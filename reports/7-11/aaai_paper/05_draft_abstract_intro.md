# Abstract + Introduction 第一稿(2026-07-11 深夜,种子定版数字)

> 状态:待导师过。所有数字与 [01_results_ledger.md](01_results_ledger.md) 一致(种子定版后)。
> 风格约束(琨哥):每段第一句 = 全段概括;不用 "in other words" 类冗词;概念先定义后使用;P1 必须带数据。

---

## Title

**Physics Is Already There: Rethinking Physical Inductive Biases in Latent World Models**

## Abstract (~185 words)

Physical state is already in the latent of a world model — injecting it again does not help. Trained without any physics supervision, a JEPA-style latent world model recovers object position by linear probing (ρ 0.80–0.96) and is near-exact for one step (latent cosine 0.98–0.99), yet its rollouts drift away from the dynamics it encodes. The popular remedy is to inject physical inductive biases into the latent. We test this systematically — five mechanism families spanning hard/soft, state/evolution, and labelled/label-free axes (ten arms), across three controlled physics domains and two photorealistic-simulation benchmarks — and find injection fails in **29 of 30** mechanism×domain cells; supplying the *correct* physical form (a = g), or co-training from scratch, hurts more rather than less (Δ +0.56 nMSE). We trace this to physical state being **decodable but not load-bearing**: the 190 non-slot dimensions redundantly encode the same position (ρ 0.78–0.92), so prediction routes around any injected slot, and re-weighting that slot restores parity in only 2 of 4 decision cells while never yielding a gain. The real gap is the predictor's long-horizon dynamics, not the latent's state content: repairing the training protocol alone — injecting no physics at all — cuts rollout error 2.2–8.3×. An extrinsic architecture closes the bypass yet still collapses under OOD physics: necessary, but not sufficient.

## Introduction(七段)

**P1 — 问题(带数据).** World models promise agents an internal simulator: given a few frames, they should predict how objects move, fall, and collide. Current latent world models fall short of this promise in a specific way — not by failing to *represent* physical state, but by failing to *evolve* it. On the PhyWorld benchmark, a JEPA-style world model (LeWM) encodes object position and velocity almost perfectly (linear-probe correlation up to 0.96), and its one-step predictions are near-exact (latent cosine 0.98–0.99). Yet when the same model is rolled out autoregressively, prediction error compounds: over 28 steps the latent cosine on collision dynamics falls to 0.24, and OOD physics (unseen radii, masses, or velocities) multiplies rollout error several-fold over the in-distribution level. State decodability does not imply physical-law compliance.

**P2 — 现状.** Two families of remedies dominate. The first injects *physical structure* into the representation: binding latent dimensions to physical quantities, attaching kinematic update equations, or supervising physics through auxiliary probes — an approach exemplified by physically-interpretable world models and deep-supervision methods, which report improved decodability and reduced drift. The second scales *data and training*, a route whose limits Kang et al. documented for video generation: diffusion-based generators trained at scale still fail OOD physics, generalizing case-by-case rather than by rule. Whether physical structure actually rescues *latent* world models — where prediction happens in representation space rather than pixel space — has not been tested systematically.

**P3 — 概念与设定.** We study this question in a controlled setting. A latent world model consists of an encoder mapping frames to embeddings and a predictor rolling embeddings forward, trained by matching predicted to encoded futures (JEPA). *Teacher forcing* trains the predictor on one-step transitions from ground-truth context; *free-rollout* trains it on its own multi-step predictions. PhyWorld provides three single-mechanism physics domains — uniform motion, parabolic flight, elastic collision — with explicit in-distribution ranges over radius/mass and velocity, yielding four evaluation partitions (ID, r/m-OOD, v-OOD, both-OOD). We report normalized MSE (nMSE) in latent space and PSNR of decoded rollouts in pixel space; we deliberately avoid cosine similarity and probe correlation as primary metrics, for reasons the paper establishes.

**P4 — 缺口.** Injection mechanisms have been evaluated in isolation — one mechanism, one domain, one initialization regime — and, crucially, never against the confound that dominates everything else: the training protocol. Prior work default-treats training choices as background and physical structure as the variable, so no study answers the basic factorial question: **does physical structure add anything once the training protocol is done right?** (A secondary hazard, which we treat as a methodological contribution rather than the main gap: probe correlation and latent cosine are *duals of the auxiliary loss* and rise mechanically when it is optimized, so they can manufacture apparent gains. We do **not** claim the literature generally relies on them — deep-supervision work reports the trustworthy pred_loss and its conclusion holds in its own low-dimensional setting; the hazard is a general warning that verdicts must rest on nMSE/pixel, not an indictment.)

**P5 — 我们做的.** We answer it by exhaustive, controlled anatomy. On a fixed backbone we cross five injection mechanisms (fixed slots, kinematic heads with both free and exact gravitational forms, deep-supervision probes, velocity-consistency losses, label-free dynamics priors) with two injection regimes (post-hoc fine-tuning and from-scratch co-training), three physics domains, and seed-controlled baselines; we then validate the surviving conclusions on photorealistic-simulation video (Physion, Physion++). We further propose a mechanistic explanation — the *load-bearing problem*: physical slots occupy 2 of 192 latent dimensions and ~1% of the prediction loss, the black-box channel redundantly encodes position, and auxiliary objectives overwhelm the prediction objective (weighted-loss ratios — a gradient-magnitude proxy — of 15–125×, collapsing the encoder's effective dimensionality by 39–90%). The explanation makes a falsifiable prediction: reweighting the loss to make slots load-bearing should remove the damage. It does — restoring parity with the baseline (0.132±0.014 vs 0.136±0.007 both-OOD nMSE) — but no more.

**P6 — 结果.** Physical structure fails everywhere; the training protocol succeeds everywhere. Every injection mechanism degrades OOD prediction in every domain (29 of 30 mechanism×domain cells at or below baseline), and the damage *grows* as baselines strengthen: with a clean from-scratch baseline, adding physics costs +0.56 nMSE on uniform motion — an order of magnitude more than the +0.035 of post-hoc grafting — closing the "structure must be baked in during pretraining" escape hatch. Even the correct physical form (learned gravity, a = g) hurts (1.57×). The mechanism is a bypass: the 190 non-slot dimensions already decode position as well as the full latent (ρ 0.78–0.92 vs. random-2D 0.2–0.5), the auxiliary term dominates the predictive one by 15–125× in weighted loss, and the encoder's participation ratio collapses 39–90% — the constraint is *acting*, just harmfully. As a controlled contrast on identical code and metrics, replacing teacher forcing with free-rollout — which injects no physics — cuts OOD error 2.2–3.6× across all three domains (seed-robust, non-overlapping intervals) and 8.3× on Physion++, while matching rollout horizon to dynamics complexity drives 64-step error to 1/19 of baseline (0.280→0.014) with no inflection.

**P7 — 贡献.** (i) The first systematic, seed-controlled anatomy of physical inductive biases in latent world models — five mechanism families, ten arms, three domains, two photorealistic-simulation benchmarks, ~200 runs — showing consistent failure (29/30). (ii) A mechanistic account, *decodable but not load-bearing*, with three falsifiable tests: a black-box bypass probe, a gradient-dominance and participation-ratio measurement that rules out the "constraint never took effect" explanation, and a dose–response re-weighting sweep. (iii) A controlled factorial showing the dominant variable is the training protocol, not physical structure — which doubles as the positive control that rules out implementation or metric failure. (iv) An architectural attribution: a faithful port of an extrinsic physics world model closes the bypass but still collapses under OOD physics (ρ 0.33 vs 0.89), making extrinsic design necessary but not sufficient.

---

## 中文备注(不进论文)

1. **P1 的 0.96/0.98–0.99/0.24/三倍**分别来自:5-26 修正协议 probe、5-27 rollout 报告、C1 表。写作时逐一挂 ledger 出处。
2. **P5 的 LBR 数字**用的是 7-11 种子定版(0.132±0.014 vs 0.136±0.007)——"恢复持平、仅此而已"的口径,比"翻正净超"更狠也更稳,审稿人挑不动。
3. **P6 的 +0.56/an order of magnitude**:uniform 干净 scratch Δ+0.558 vs uniform 后训练嫁接 Δ+0.035(注意二者 epoch 不同 60/20,正文写清 setup)。⚠️ 原稿的"8×(parabola +0.550)"已作废——parabola both-OOD 被 h28 除零爆点污染(lewm 交接 NOTE),parabola 一律用 r/m-OOD 判决(C1 里 FR/TF = 0.122±0.006 / 0.443±0.047 → 3.6×,即 P6 区间上限的来源)。
4. Related work 段(P2)按 02 §4 的台账展开,写作时先跑 citation audit(三篇 [UNVERIFIED])。
5. "We release all code..." 若不打算开源,删掉或改为 protocols in appendix。
6. deform 短板、单 backbone 限制放 §7 Discussion,不进 intro。